# Configuration fragment only; do not import as a complete configuration.
# Copy the required HOST rule into your existing [filter_local], before FINAL.
# Replace the reserved example domain with the exact hostname from your log.
# Pick the original working policy: direct for direct traffic, AI for AI traffic.
# AI must already exist in your configuration; otherwise use your own policy name.
[filter_local]
HOST,ads.example.com,direct
HOST,assets.example.net,AI
